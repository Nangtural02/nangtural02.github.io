let int bannum = 0;
const bannerStyle = document.createElement("span");
const addBanner = document.createElement("ul");
const bannerList = document.createElement("li");

bannerStyle.appendChild(addBanner);
for(int i=0; i<bannum; i++){
addBanner.appendChild(bannerList);
}
